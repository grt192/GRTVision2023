# takes in images 
# returns position estimates in array form
# [ (tagid, prediction), (tagid, prediction)...]
# ex: [ (21, 0, 1.5, 1), (12, 1, 2.5, 1) ]

class AprilTags:
    def __init__(self):
        return

    def get(self, image):
        return